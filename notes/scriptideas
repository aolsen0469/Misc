#################################
	BASH SCRIPT IDEAS
#################################
-- Tool to reformat //media server formats.
	- ie move /media-hdd/television/arrow/blah.mov -> 
		  /media-hdd/television/arrow/season1/blah.mov

-- Nagios Tool : to quickly add hosts

-- IPTABLES Tool

--Script to create a VM from scratch
	--Script to clone vm
--Script that deletes VM. [remove ls] **

--Script that cats VM info [ipaddresses, etc]

--Script that makes new user, adds them to group, etc

-- Script that ssh's into VM & changes resolv.conf

-- Script that alters .ldiffiles changes dc=example,dc=com into dc=hpserver,dc=net

--Script tool for applying .ldif changes

--Script that turns on and turns off all essential VMs **
((AKA DHCP,Spacewalk,DNS))
